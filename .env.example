# Optional. Points the API at a different SQLite database file without any
# code change. Defaults to data/processed/portfolio.db if unset -- the demo
# below overrides it to run against the committed synthetic dataset instead.
# PORTFOLIO_DB_PATH=demo-data/portfolio.demo.db
