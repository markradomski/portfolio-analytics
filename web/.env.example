# Optional. Base URL the frontend calls for the API. Defaults to
# http://localhost:8000, which matches `uvicorn src.api.app:app` run with
# its own default port.
# VITE_API_BASE_URL=http://localhost:8000
